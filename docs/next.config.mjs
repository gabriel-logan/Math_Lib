/** @type {import('next').NextConfig} */
const nextConfig = {
	output: "export",
	images: {
		unoptimized: true,
	},
	assetPrefix: "/Math_Lib/",
	basePath: "/Math_Lib/",
};

export default nextConfig;
